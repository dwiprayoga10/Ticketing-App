<x-layouts.admin title="Tambah Lokasi">

    <div class="container mx-auto p-10 max-w-xl">
        <h1 class="text-3xl font-semibold mb-6">Tambah Lokasi</h1>

        <form
            action="{{ route('admin.lokasi.store') }}"
            method="POST"
            class="bg-white p-6 rounded-box shadow-xs"
        >
            @csrf

            {{-- Nama Lokasi --}}
            <div class="mb-6">
                <label class="label">
                    <span class="label-text">Nama Lokasi</span>
                </label>
                <input
                    type="text"
                    name="nama_lokasi"
                    class="input input-bordered w-full"
                    value="{{ old('nama_lokasi') }}"
                    required
                >
                @error('nama_lokasi')
                    <span class="text-red-500 text-sm">{{ $message }}</span>
                @enderror
            </div>

            {{-- Status default (hidden) --}}
            <input type="hidden" name="aktif" value="Y">

            {{-- Action --}}
            <div class="flex justify-end gap-2">
                <a href="{{ route('admin.lokasi.index') }}" class="btn">
                    Kembali
                </a>
                <button type="submit" class="btn btn-primary">
                    Simpan
                </button>
            </div>

        </form>
    </div>

</x-layouts.admin>
 
