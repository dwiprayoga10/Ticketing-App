<x-layouts.admin title="Manajemen Lokasi">

    {{-- Toast Success --}}
    @if (session('success'))
        <div class="toast toast-bottom toast-center">
            <div class="alert alert-success">
                <span>{{ session('success') }}</span>
            </div>
        </div>

        <script>
            setTimeout(() => {
                document.querySelector('.toast')?.remove()
            }, 3000)
        </script>
    @endif

    <div class="container mx-auto p-10">
        <div class="flex items-center mb-4">
            <h1 class="text-3xl font-semibold">Manajemen Lokasi</h1>
            <a href="{{ route('admin.lokasi.create') }}" class="btn btn-primary ml-auto">
                Tambah Lokasi
            </a>
        </div>

        <div class="overflow-x-auto rounded-box bg-white p-5 shadow-xs">
            <table class="table">
                <thead>
                    <tr>
                        <th>No</th>
                        <th>Nama Lokasi</th>
                        <th>Status</th>
                        <th width="20%">Aksi</th>
                    </tr>
                </thead>
                <tbody>

                    @forelse (($lokasis ?? collect()) as $index => $lokasi)
                        <tr>
                            <th>{{ $index + 1 }}</th>
                            <td>{{ $lokasi->nama_lokasi ?? '-' }}</td>
                            <td>
                                @if(($lokasi->aktif ?? 'N') === 'Y')
                                    <span class="badge badge-success">Aktif</span>
                                @else
                                    <span class="badge badge-ghost">Nonaktif</span>
                                @endif
                            </td>
                            <td>
                                <a href="{{ route('admin.lokasi.edit', $lokasi->id ?? 0) }}"
                                   class="btn btn-sm btn-primary mr-2">
                                    Edit
                                </a>

                                <button
                                    class="btn btn-sm bg-red-500 text-white"
                                    onclick="openDeleteModal(this)"
                                    data-id="{{ $lokasi->id ?? 0 }}">
                                    Hapus
                                </button>
                            </td>
                        </tr>
                    @empty
                        <tr>
                            <td colspan="4" class="text-center">
                                Tidak ada data lokasi.
                            </td>
                        </tr>
                    @endforelse

                </tbody>
            </table>
        </div>
    </div>

    {{-- Modal Hapus --}}
    <dialog id="delete_modal" class="modal">
        <form method="POST" class="modal-box">
            @csrf
            @method('DELETE')

            <h3 class="text-lg font-bold mb-4">Nonaktifkan Lokasi</h3>
            <p>Yakin ingin menonaktifkan lokasi ini?</p>

            <div class="modal-action">
                <button class="btn btn-primary" type="submit">
                    Ya, Nonaktifkan
                </button>
                <button type="button" class="btn" onclick="delete_modal.close()">
                    Batal
                </button>
            </div>
        </form>
    </dialog>

    <script>
        function openDeleteModal(button) {
            const id = button.dataset.id
            if (!id) return

            const form = document.querySelector('#delete_modal form')
            form.action = `/admin/lokasi/${id}`
            delete_modal.showModal()
        }
    </script>

</x-layouts.admin>
